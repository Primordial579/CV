sudo apt update
sudo apt install ansible -y

sudo nano /etc/ansible/hosts

In terminal:

[local]
localhost ansible_connection=local

Execute(ctrl+t): ansible localhost -m ping

mkdir abc
cd abc

nano abc.yml

In terminal:

---
- name: First yml
  hosts: localhost
  
  tasks:
  - name: Test connectivity
    ping:
  - name: "Hello world"
    debug: msg="this is a debug message"

Exit and save(ctrl+x)


ansible-playbook abc.yml